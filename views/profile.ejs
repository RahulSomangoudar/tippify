<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            height: 100vh;
            background: url('https://t3.ftcdn.net/jpg/03/44/52/88/360_F_344528805_o1LwyPDkkDZ7TEbTE5J08e9m00ZifmGK.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header .header-left h1 {
            margin: 0;
        }

        header .header-right {
            margin-left: auto;
        }

        .container {
            display: flex;
            flex: 1;
        }

        aside {
            background-color: #2b2b2b;
            width: 200px;
            padding: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }

        aside nav ul {
            list-style: none;
            padding: 10px;
            margin: 10px;
        }

        aside nav ul li {
            margin-bottom: 30px;
        }

        aside nav ul li a {
            text-decoration: none;
            color: white;
            padding: 10px;
        }

        aside nav ul li a:hover {
            background-color: rgb(162, 104, 255);
            border-radius: 5px;
        }

        .content {
            flex: 1;
            padding: 20px;
        }

        img {
            margin-top: 90px;
            max-width: 100%;
            height: 300px;
        }

        h1 {
            margin-left: 20px;
        }

        button {
            
            height: 44px;
            margin-left: -233px;
            margin-top: 420px;
            border-radius: 10px;
            background-color: rgb(162, 104, 255);
            border: 0px;
        }

        button a {
            text-decoration: none;
            font-size: 17px;
            color: #fff;
        }

        button a:hover {
            background-color: rgb(126, 84, 255);
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>
        <div class="container">
            <%- include('partials/sidebar') %>
                <h1>Welcome to your profile, <%= user.name %>!</h1>
                <img src="<%= user.qrCodePath %>" alt="QR Code">
                <button>
                    <a href="<%= user.qrCodePath %>" download="qr.png">Download QR Code</a>
                </button>

        </div>
        <script>
            document.querySelector('.download-btn a').addEventListener('click', function (event) {
                console.log('QR Code Downloaded');
            });
        </script>
</body>

</html>